{{-- The working-tenant switcher: the per-tenant logo/name that opens a dropdown of accessible sites
     (single-site operators see a static chip). Session tenant is the same state EnsureTenantSelected manages. --}}
@livewire(\App\Livewire\TenantSwitcher::class)
